# /refactor

Refactor one bounded area only.

Rules:
- preserve behavior
- avoid speculative rewrites
- reduce duplication
- improve naming
- improve component reuse
- run regression checks

Do not combine unrelated refactors.
