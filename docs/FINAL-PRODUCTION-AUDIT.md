# Final Production Audit

Run `/finalize` to generate the evidence-based production readiness report.

> **Pre-conditions for `/finalize`:** all secrets rotated per `SECURITY.md` §3;
> all phases 0–5 complete; all open items in `ROADMAP.md` resolved; CI green;
> manual smoke test passed on every macro.

---

## Status (post Phase 0 audit + Guardian Phase 1+2+3 + Sidebar Phase 4)

Production readiness: **NOT READY** (still multiple blockers).

Resolved since initial audit:

- [x] Guardian API implemented (Phase 1)
- [x] Guardian UI page at `/sanctum/guardian` (Phase 2)
- [x] Guardian event-driven push (Phase 3)
- [x] Guardian pre-trade banner in Journal (Phase 3)
- [x] Guardian widget on Dashboard (Phase 3)
- [x] Daily-loss signals (Phase 3)
- [x] `/sanctum` security rule fixed
- [x] Sidebar restructured to 5 macros + Admin (Phase 4)
- [x] Notifications sidebar duplication resolved (single location: Mi Cuenta)
- [x] Home/Login split (Phase 5)
- [x] CSS extraction from home.html.twig → `assets/styles/home.css` (Phase 5)
- [x] Public/Sanctum shell split (Phase 5 — public shell loads only its own CSS)
- [x] Deploy tooling created: `bin/{pre-deploy-check,deploy,post-deploy-smoke,rollback}.sh` (Phase 6)
- [x] `.gitignore` hardened: `stitch_tnsvt_app_m_vil/`, `public/uploads/`, IDE files
- [x] `docs/DEPLOYMENT.md` complete guide
- [x] CI pipeline: `.github/workflows/ci.yml` (Phase 7)
- [x] Settings split — Account created at `/account/settings` (Phase 7)
- [x] `shell.html.twig` inline CSS extracted to `assets/styles/shell.css` (Phase 8)
- [x] CSS consolidation plan documented in `DESIGN-SYSTEM.md` (actual merge deferred — requires running app for visual verification)
- [x] CSS Merge 1 (tokens) complete (Phase 9)
- [x] CSS Merge 2 (glow) complete (Phase 9)
- [x] CSS Merge 3 (components) complete (Phase 9)
- [x] CSS bundle consolidated: 14 → 9 canonical files (60% reduction)
- [x] Phase 10 — legacy `--v2-*` aliases removed; `components.css` migrated to
  `-elev` vars; missing tokens added to `tokens.css`; 0 `var(--v2-*)` refs remain

Remaining blockers:

- [ ] **Secrets rotation pending** (see `SECURITY.md` — manual operator action)
- [ ] Manual smoke test of Guardian + new public shell on a real account
- [ ] Deploy Phase 4+ code to tnsvt.com (current production shows OLD layout)
- [ ] `/trading` still legacy placeholder (deferred per decision)
- [ ] Mentor area not implemented
- [ ] Health endpoint for monitoring

---

This document will be **regenerated by `/finalize`** after all blockers are
resolved and every checklist item has evidence.
