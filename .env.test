# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"

# MercadoPago webhook tests (fake credentials; never used to make real API calls)
MP_ACCESS_TOKEN=TEST-FAKE-TOKEN-FOR-SIGNATURE-VERIFICATION-ONLY
MP_WEBHOOK_SECRET=test_webhook_secret_for_signing_verification

# Trusted proxies for tests (allow X-Forwarded-For from localhost)
TRUSTED_PROXIES=127.0.0.1,REMOTE_ADDR

# Messenger / Mercure test config
MESSENGER_TRANSPORT_DSN=in-memory://
MERCURE_URL=http://127.0.0.1:3000/.well-known/mercure
MERCURE_JWT_SECRET=test-mercure-secret-32bytes-placeholder